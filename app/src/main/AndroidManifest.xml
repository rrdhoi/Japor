<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bangkit.japorapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JaporApp"
        tools:ignore="AllowBackup">
        <activity android:name=".ui.splashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ReportActivity" />
        <activity android:name=".ui.onboarding.OnboardingFourActivity" />
        <activity android:name=".ui.onboarding.OnboardingThreeActivity" />
        <activity android:name=".ui.onboarding.OnboardingTwoActivity" />
        <activity android:name=".ui.onboarding.OnboardingOneActivity" />
        <activity android:name=".ui.HomeActivity"/>
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>